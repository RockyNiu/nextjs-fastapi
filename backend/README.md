# backend

## install
```bash
pip install poetry # TIP: install poetry through admin privileges on Windows.
poetry shell # activate virtual environment, if not already activated.
poetry install # install dependencies.
```